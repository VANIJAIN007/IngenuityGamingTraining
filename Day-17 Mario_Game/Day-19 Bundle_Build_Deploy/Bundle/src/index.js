// in ES6 we use require to import library
const lib=require('./library');
const loadash=require('loadash');
console.log(lib.add(5,10));
console.log(loadash.uppercase("jeiro"));
console.log(loadash.add(50,260));

